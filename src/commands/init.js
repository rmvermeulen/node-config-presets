// @ts-check

/** @function init
 * Add or ignore configs one-by-one
 * and get only the required dependencies as output
 */
module.exports = async presetDir => {
  throw new Error(`Command 'init' not implemented`);
};
